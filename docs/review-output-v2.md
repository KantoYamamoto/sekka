# レビュー出力の改善をPR #81で再確認

2026-09-06。ローカルGitにある `a2fdbce859d3` → `18ac7b2f9cba` を読み、Patchwork 0.2の出力を検証した。対象アプリのソース・作業ツリーは編集せず、ビルド・テスト・GitHubへの投稿も行っていない。これはPatchworkの出力検証であり、PRの正しさの再レビューではない。

## 確認結果

- 解析対象はSwift 41→41ファイル。実際に変更された入力Swiftは10ファイル。
- 構造観測は従来と同じ17件、7ファイルから出た。
- 観測がない3ファイル（HomeView / LearningSessionView / SummaryView）を冒頭とJSONに列挙できた。
- 変更ファイル内の対応可能な本体322組を比較し、314組のtoken列が同一、8組はtoken列だけが変化した。構造指標が同じでも通常diffを確認する入口ができた。
- 本体比較を行わない18項目を理由・前後位置付きで表示した。引数変更による省略と、新しいメソッドに比較相手がないケースなどを含む。これは未検証のメソッド総数や網羅率ではない。対応不能な旧・新宣言は別項目になる場合がある。
- presentationMode / displayedLanguage等の引数追加を要約できた。引数とアクセス修飾子が同時に変わるケースでは、その両方を残した。
- compactとfullでcoverage・limitationsが同一であることを確認した。

## 出力量（文字数）

| 出力 | 文字数 |
| --- | ---: |
| 試用レポートに保存されていた旧JSON | 56,592 |
| 新compact JSON | 29,817 |
| 新full JSON（比較範囲情報を含む） | 86,032 |
| 新text（比較範囲情報を含む） | 12,220 |

旧JSONに対してcompactは47.3%減。文字数による比較で、モデルごとのtoken数や費用削減率を測ったものではない。textは型をまとめた一方、以前見えなかった比較範囲も加わるため、旧textの10,336文字より長い。

## 対照実験

LearningProgressServiceの前後を一時ディレクトリへ読み出し、after側の形式選択だけを旧式へ戻した別コピーも比較した。両方とも構造観測は2件のまま。ただし両方に、makeRecommendationの引数節変更により本体比較を省略したという情報が出ることを確認した。

今回の改善は、誤った実装を自動的に区別できるようにするものではない。比較できなかった場所や、数値に現れない本体変更を黙って見落とさないための改善である。

## 出力の確認先

再実行結果は作業ディレクトリの `.build/review-validation/` に保存した（Git管理外）。アプリのソースを含む生出力をPatchworkのリポジトリには追加していない。汎用サンプルの新出力は [demo-output.txt](demo-output.txt) に保存している。
