# bloom-timelapse: 提供された試用報告と次の手

2026-09-07。ユーザー提供のレビュー担当者の報告。評価器は `e76a5641`、Swift 6.3.3。以下の数値は報告値であり、開発担当による再計測ではない。

| PR | Sekka text / Swift diff | 報告された利益と負担 |
| --- | --- | --- |
| #43 | 54行・3.8KB / 130行・5.1KB | 場所は当たるが二度読みが割に合わない |
| #56 | 182行・11.1KB / 705行・27.6KB | 設定型・画面・取込経路・テストの入口として有用 |
| #52 | 224行・14.9KB / 962行・36.5KB | 構造は見えるが新規431行の中心処理は未比較 |
| #39 | 373行・24.8KB / 1,811行・74.3KB | 圧縮するが40観測・57未比較本体で出力も重い |
| #46 | Swift変更0 / 文書4ファイル | CLIに非Swift変更の存在が出ない |

実行は各約1秒との報告。「索引7/10・レビューツール4/10」「300行以上なら使いたい」は個人の主観であり、導入閾値にはしない。通常diffの代替にならないこと、数式・閾値・ロールバックの意味を判断できないことは現行スコープの限界である。

## 開発側で確認したこと

- 評価器は初期値変更の案内を追加したPR #27を含む。現行PR #33の既存型表記の再掲は含まない。初期値フォールバックを単に旧版の問題とは扱えない。
- GitHub APIで#46が文書4ファイルだけの変更であることを確認した。現行バイナリで「Swiftファイルは存在するがREADMEだけ変更」の一時Git入力を実行すると、READMEが出ず `0 observations` となった。省略問題は現行でも再現する。
- CLIの入力はSwiftファイルだけで、PR用スクリプトが別のmanifestから全変更一覧を補っている。共有する入力契約を直す必要がある。
- 新規本体の「比較相手なし」は構文解析そのものの失敗ではない。前後比較・構文事実・意味の検証を表示で区別する必要がある。
- 初期値フォールバックの具体的なコマンド・場所は未再現。既存の長い初期値のナビゲーションテストが通ることだけで報告を否定しない。

## 次の作業

[判断0023](../decisions/0023-review-entry.md)に従い、[#34](https://github.com/KantoYamamoto/sekka/issues/34)全変更一覧、[#35](https://github.com/KantoYamamoto/sekka/issues/35)確認先の読み順、[#36](https://github.com/KantoYamamoto/sekka/issues/36)初期値の再現・表示比較の順に進める。

ユーザーは、この整理後に独立したAI 2件でのA/B比較も実施するよう許可した。準備済みのNuke入力は現行版の基準点としてそのまま使う。この報告は外部からの利用経験として価値があるが、同条件A/B比較やM2全体の完了には数えない。
