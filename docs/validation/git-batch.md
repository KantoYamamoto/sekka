# Git一括読み取りの検証

2026-09-07記録。実装`5f9313d`、Issue #13。入力はSekkaの`1f8e4af`→`b42b2cd`。旧評価器はPR03時点の退避バイナリ、新評価器はバッチ実装。ローカルでold/newの順序を交互にして3回ずつ実行した。

| 評価器 | 1回目 | 2回目 | 3回目 | 中央値 |
| --- | ---: | ---: | ---: | ---: |
| 旧・ファイル単位起動 | 9.132秒 | 8.578秒 | 8.566秒 | 8.578秒 |
| 新・バッチ取得 | 1.900秒 | 1.719秒 | 1.750秒 | 1.750秒 |

全6実行でstdoutがバイト単位で一致。合成fixture、既知PR #81/#78でもPR03のtext/JSONとバイト単位で一致した。キャッシュを消したcold-start試験ではなく、この環境のツール実行時間。レビュー時間の改善率とは呼ばない。

Swiftテスト41件、CLIチェック27件通過。UTF-8のバイト長、空blob、改行なし、本文中のヘッダー風文字列、不正な応答、同一blob複数パスを検証した。

## 自己利用

親`bd6396b`→実装を退避評価器と比較。SekkaからGitBatch追加とInputs.gitSnapshot/runGitの変更へ進めた。通常diffでバイト境界の検証と一時stdinの後片付けを確認し、トップレベルテストは通常diffで読んだ。解析結果をキャッシュする仕組みを増やさず、プロセス起動数を減らす変更として確認できた。

次はユーザーの追加依頼であるGitHub ActionsのPR表示を整え、この改善PRでも実際のチェック・要約・成果物を確認してからマージする。M2の独立した比較は引き続き未完了。
