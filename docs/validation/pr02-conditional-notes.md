# PR02: 共通NOTE集約の検証

2026-09-06。実装`2bb54be`。Issue #3。旧評価器は`a97138a`、同じ固定入力を使用。

| ケース | 0.2旧text文字数 | PR01+02後 |
| --- | ---: | ---: |
| synthetic | 7137 | 3057 |
| pr81 | 12204 | 8323 |
| pr78 | 6474 | 3902 |

3ケースとも旧compact JSONと全フィールドが一致。前後別のNOTE位置・比較省略・観測を保持した。Swiftテスト35件、CLIチェック18件通過。

## 自己利用

親`4d2d419`→実装をPR01後の旧評価器と比較。Sekkaから`Notice`と`DiffReport`の内部状態追加、`NoticePresentation`追加へ進めた。通常diffでCodingKeysを照合し、内部情報を既存JSONへ出していないことを確認した。トップレベルテストは通常diffで確認。

未変更fixture由来の22行の共通NOTEが1行になった。一方、`DeclarationVisitor.visit`はオーバーロードされたvisit群で本体の対応が曖昧になりやすく、場所を示しても該当変更を探す作業は残る。次はIssue #4でdiff導線を付ける。ヘッダー照合はブロック同一性の証明ではなく、その限界も表示する。

これは実装者による既知差分の自己レビュー。出力量の減少は確認できたが、独立したレビュー時間短縮は未検証。
