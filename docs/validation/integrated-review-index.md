# 確認先の統合: #35

既知のbloom-timelapse PR #43/#52/#39を、全変更一覧導入後の旧バイナリと表示変更後のバイナリで比較した。過去の提供報告とは評価器が異なる。生出力・固定SHAはignoredの`.build/review-entry/{43,52,39}`に保持し、私有コードは公開しない。対象アプリのビルドはしていない。

| 入力 | text行数 旧→新 | UTF-8 bytes 旧→新 | 確認できたこと |
| --- | ---: | ---: | --- |
| #43 | 63→60 | 4,243→4,232 | 構造観測0件と本体token変更が同じ行で読める |
| #52 | 244→245 | 15,811→16,098 | 新規型を含むファイルの未比較本体数が入口に出て、集約にもafter行番号がある |
| #39 | 465→450 | 28,638→28,615 | 本体token変更の説明を繰り返さず、24パスの表示省略数も維持 |

3件ともfull JSONは旧版と完全一致した。型別の宣言・引数情報を維持している。#52は行番号・ファイル状態を補うため増量し、全体の大幅な省トークンにはなっていない。ここで確認したのは読み順・位置・事実保持であり、既読入力の再読をレビュー時間短縮の証拠にはしない。

合成例では、同じ観測0件でも本体token変更・個別案内のないトップレベル構文変更・コメントだけの変更を区別した。追加・削除12本体の位置は該当側の先頭3件と残り9件になり、全12本体のJSON記録を保持する。新規本体への`--at`が全hunkへ戻る制約は#36で検証する。

Swift 60テスト、CLI/Git 39チェック、Python 13テストが通過。PR表示の固定例でもSwiftとその他の件数を照合した。実装後の自己利用・Actions・実PR表示確認は、この記録とPR本文へ結果を残す。

`9961210`対親`e5c94d4`を新旧評価器で自己利用し、通常Swift diff全体と両出力を読んだ。full JSONは完全一致し、観測3件は表示クラスの追加と本体構造変化を示した。ファイル状態の集計・位置順・表示上限の正しさは通常diffとテストで確認した。別の実装レビュアーも表示の欠落とHTML境界を点検し、重要な指摘はなかった。これは未読PRでのレビュー利益比較ではない。
