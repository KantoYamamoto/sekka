# Sekka自身のPR: 提供された試用報告

記録日2026-09-08。ユーザー提供の別レビュー。評価器の正確なSHA・実行コマンド・生出力は未提供であり、以下は再計測値ではない。

| 場面 | 報告された利益 | 報告された限界 |
| --- | --- | --- |
| PR #14 | DiffNavigation、fingerprint、endLine、CLI引数の追加を一画面で把握 | 新規ファイルの `--at` が122行全体へ戻る |
| PR #16 | 既存メソッドの関連hunkへ正しく移動 | バッチ化やデコードの意味はdiffが必要。3ファイルなら直接読む方が早い |
| PR #27 | 引数・プロパティ追加を検出 | 初期値検出という実装目的は説明できず、テストは構造観測なし |
| PR #33 | Findingなどの既存表記には文脈の価値がある | String、Int?、[String]の再掲はノイズが強い |

追加の指摘は、changed-syntax-onlyの低い情報量、新規本体の未比較、変更と無関係なfixtureの条件コンパイルnotice 22件、PR表の件数単位の混在、10ファイルを超える一覧の再掲感、初回ビルド約2分半の導入負担。実行は1秒未満との評価だった。

## 計画への統合

- 全変更を把握できることと全件を常に展開することを分ける。#34は情報の完全性、#35は短い入口と単位の明記を担う。非Swiftを黙って消す案にも、無制限に並べる案にも寄せない。
- #36を既存初期値だけでなく新規宣言のナビゲーションも含む再現検証に改める。対応がない前後比較と、片側の場所を案内できるかは別の問題。
- 既存型表記を残すことを既定路線にせず、現行・省略・要求時だけ表示を対照例で比較する。JSONの共通制約と個別noticeも同じ読む負担の検証に入れる。
- 配布は限定的な試用導線として記録する。全PRへの常設botやHomebrewまで一度に広げない。

大きなAPI・モデル変更の事前資料には利益がある一方、実装目的の説明や設計判断を出せるとは確認できていない。元の目的は維持するが、その達成を「構文が数えられる」ことで代用しない。opt-in CLI/artifactを当面の利用仮説とし、常設botはSekka自身での検証用途に留める。

実施中の独立A/B担当には、この報告や開発側の解釈を渡していない。
