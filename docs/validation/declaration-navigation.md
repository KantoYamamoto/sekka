# 初期値と片側宣言への移動: #36

2026-09-08。まず表示変更版9961210で8合成ケースと公開Sekka PR #14を再現した。合成入力・出力はignoredの`.build/navigation-study`へ保存した。元の試用報告には指定行がないため、当時の操作そのものの再現とは断定しない。

| 入力と選択 | 変更前 | 原因と選択 |
| --- | --- | --- |
| 短い式3→8・長い配列、after:2 | 無関係な関数を含む全2hunk | propertyを本体比較から探しており選択できない。既存binding範囲を使う |
| 式の追加・削除、before/after:2 | 全2hunk | 宣言自体は両側にあるので同じ範囲選択を使う |
| 同一行の複数binding・同名property重複 | 全2hunk | 選択が曖昧。フォールバックを維持 |
| 既存型へのメソッド追加、after:3 | 全2hunk | 比較相手なしを移動不可とも扱っていた。片側だけの範囲を許可 |
| 新規ファイルのメソッド、after:2 | 選択成功、全1hunk | 1hunkに全ファイルが含まれる。宣言境界では切り取らないと明示 |
| Sekka PR #14、DiffNavigation.swift after:5 | 選択成功、全1hunk・122追加行 | 同上。宣言の照合失敗ではない |

PR #14の固定入力は`1f8e4af94fac1fb5530e0648b66ead5d674de554` → `0d1c029b10a3be978a196931899caea0e1692f46`。現在のSekkaを使い、通常のtextで得た入力IDと`--show-diff Sources/SekkaCore/DiffNavigation.swift --at after:5`を指定した。

比較した表示案は、短い式の前後再掲・長い式の全再掲・宣言に重なる既存hunkの選択。短い式は読みやすいが、長い式では未変更の20要素も繰り返す。今回はhunk選択を直す。前後式を自動text/JSONへ追加しない判断と、新規ファイルのhunk粒度の制約は[0028](../decisions/0028-declaration-navigation.md)へ残した。

実装後は短い/長い初期値と式の追加削除が1/2hunkになり、無関係な関数を除外する。既存型への追加と削除も片側の1/2hunkへ進む。複数binding・重複property・重複型は全ファイルへ戻る。新規ファイルは1/1hunkを明示し、宣言だけを切り出したとは表示しない。Swift 63テストで確認した。

CLI/Git 39チェックも通過。`f97aa68`対親`8532f2a`を新旧評価器で自己利用し、通常Swift diff全体を読んだ。full JSONは完全一致、観測4件は範囲選択helperと型の追加を案内した。対応付けの安全性は通常diffと境界テスト、絞り込み効果は8ケースの再実行で確認した。依頼した独立実装レビューは担当の利用上限で完了せず、独立承認として数えない。
